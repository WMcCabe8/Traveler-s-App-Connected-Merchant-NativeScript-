<Page loaded="load">
  <GridLayout rows="auto, *">
      <ListView items="{{ merchants }}" itemTap="toMerchant" row="1">
          <ListView.itemTemplate>
              <Label text="{{ storename }}" />
          </ListView.itemTemplate>
      </ListView>
  </GridLayout>
</Page>